package constant

const (
	JwtExpireTime = 86400
	JwtKey        = "!@#$%^&()sinking" //token加密key(必须为16位)
	JwtTokenName  = "token"            //用户token名称
	JwtDeviceName = "device"           //jwt类型
)
